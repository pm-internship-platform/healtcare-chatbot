# backend/setup.py
from setuptools import setup, find_packages

setup(
    name="health_chatbot",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Your requirements will be installed from requirements.txt
    ],
)